import React from "react";
// import { isAuthenticate } from "../auth/helper/index";
import Base from "../core/Base";

const UserDashBoard = () => {
  return (
    <Base title="User Dashboard" description="User Profile Pages">
      <h1>Welcome to </h1>
    </Base>
  );
};

export default UserDashBoard;
